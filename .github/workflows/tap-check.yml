name: Tap Check

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read
  issues: write
  id-token: write

jobs:
  check:
    uses: kungfu-systems/buildchain/.github/workflows/build.yml@v2
    with:
      buildchain-alpha-contract-lock-path: buildchain.alpha-contract-lock.json
      buildchain-stable-contract-lock-path: buildchain.contract-lock.json
      buildchain-contract-compatibility-policy: major-compatible
      buildchain-contract-drift-issue-mode: compatible-and-breaking
      runner-preset: custom
      platforms-json: >-
        [{"id":"linux-x64","name":"Linux x64","runner":"[\"ubuntu-24.04\"]"}]
      require-build: false
      require-verify: true
      verify-command: node scripts/check-tap.mjs
      sample-process-tree: false
      artifact-name: homebrew-tap-check
      artifact-paths: |
        tap-manifest.json
        buildchain.alpha-contract-lock.json
        buildchain.contract-lock.json
        package.json
        Formula
        Casks
        docs
        kfd
      expected-artifacts-json: >-
        {"requiredPaths":["tap-manifest.json","buildchain.alpha-contract-lock.json","buildchain.contract-lock.json","package.json","Formula/buildchain.rb","docs/MAP.md","kfd/kfd-1.witness.json","kfd/kfd-2.release-claims.json","kfd/kfd-3.witness.json"]}
